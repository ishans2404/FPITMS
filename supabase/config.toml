# Minimal local-dev config. Your hosted project (iqeadxvtmxwxevvhcmvu) is already
# linked via the Supabase GitHub integration described in your message — this
# file matters mainly for `supabase start` / `supabase db push` locally.
project_id = "fpitms"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:5173"
additional_redirect_urls = ["http://localhost:5173"]
jwt_expiry = 3600
enable_signup = false
